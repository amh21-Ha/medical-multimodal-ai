apiVersion: apps/v1
kind: Deployment
metadata:
  name: multimodal-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: multimodal-redis
  template:
    metadata:
      labels:
        app: multimodal-redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: multimodal-redis
spec:
  selector:
    app: multimodal-redis
  ports:
    - port: 6379
      targetPort: 6379
